# AGENTS.md — ai-agent-news 수집 에이전트 규칙

이 리포에서 뉴스를 수집·기록하는 에이전트(대구루, 레노버)가 따르는 규칙.
Comp AI CRM의 증거 장부(evidence ledger) 패턴을 축소 적용한 것. 배경: `context/records/_template.md`의 "증거 규칙" 섹션.

## 증거 3원칙

1. **관찰/판단 분리** — 원문에 있는 것만 사실로 기록한다. 해석·추측은 심층 분석 섹션에 명시적으로 둔다. 소급 불가능한 사실 문장은 쓰지 않는다.
2. **발췌가 근거** — 원문 발췌는 저작권 인용이자 근거 기록이다. 핵심 요약의 사실 문장은 발췌로 거슬러 올라갈 수 있어야 한다.
3. **선정 이유 기록** — 수집 노트(선정 이유 + 제외 후보) 없이는 커밋하지 않는다. 이유 없는 선정은 디폴트일 뿐이다.

## 금지

- 확신도·신뢰도 점수 같은 자기 평가 ("확실함", "신뢰도 높음" 등)
- 중요도 ⭐를 직감으로 매기기 — 반드시 템플릿의 산정 규칙(소스 권위 + 교차 확인 + 반응 규모)으로 계산
- 속성 없는 주장 전달 (누가 말한 것인지 명시할 것)

## 절차

1. 탐색 → 후보 목록 확보 → 선정/제외 결정 (제외 이유는 수집 노트에 남김)
2. 원문 fetch → 발췌 확보 → 요약·번역·분석 작성 (사실은 발췌로 소급 가능하게)
3. 메타데이터 기입: 소스 권위, 교차 확인 개수, 중요도(산정표 계산)
4. `python3 scripts/lint_records.py` 실행 → **error 0 확인**
5. 커밋 (메시지 예: `📰 2026-09-11: 뉴스 수집 (N건)`)

## 적용 기준

- 2026-09-11 이후 수집분부터 신규 필드(소스 권위, 교차 확인, 수집 노트) 필수.
- 2026-09-10 이전 레코드는 소급 수정 금지. lint가 legacy 경고로만 표시한다.

## 위키 컴파일

기존 관행 유지: `context/records/`의 누적 레코드를 주제별 `context/wiki/*.md`로 정리. 위키에서도 사실/해석 구분을 유지하고, 레코드로 소급 가능한 문장만 사실로 쓴다.
